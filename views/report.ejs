<!DOCTYPE html>
<html lang="en">
<head>
  <% include partials/head %>
  <style>
    header, main, footer {
      padding-left: 300px;
    }

    @media only screen and (max-width : 992px) {
      header, main, footer {
        padding-left: 0;
      }
    }
  </style>
</head>
<body>
  <header>
    <% include partials/navbar %>
    <% include partials/sidenav %>
  </header>
  <main>
    <div class="content valign-wrapper">
      <div class="container">
      <% if(user){%>
        <h4 class="center-align">Keluhan</h4>
        <div class="row">
          <div class="col s12">
            <div class="card-panel z-depth-2">
              <form method="post">
                <input type="hidden" name="status" value="<%=reports.status%>"/>
                <span class="new badge <%=(reports.status) ? 'deep-orange':'teal'%>" data-badge-caption="<%=(reports.status) ? 'Open':'Close'%>"></span>
                <p>Kategori: <b><%=reports.categoryName%></b></p>
                <p>Unit: <b><%=reports.unitName%></b></p>
                <p>Tanggal: <b><%=reports.tanggal%></b></p>
                <%if (user["role"] == 1){%>
                <button class="btn waves-effect waves-light btn-small" type="submit">Ubah status</button>
                <%}%>
                <p>Komplain: <b><%=reports.masalah%></b></p>
              </form>
            </div>
          </div>
        </div>
        <%}%>
      </div>
    </div>
  </main>
  <footer>
    
  </footer>
  <% include partials/script %>
</body>
</html>